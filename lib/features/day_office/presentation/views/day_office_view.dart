import 'package:flutter/material.dart';

class DayOfficeView extends StatelessWidget {
  const DayOfficeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Day Office'));
  }
}
