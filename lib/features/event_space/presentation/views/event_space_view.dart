import 'package:flutter/material.dart';

class EventSpaceView extends StatelessWidget {
  const EventSpaceView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Event Space'));
  }
}
