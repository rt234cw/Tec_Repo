import 'package:flutter/material.dart';

class CoworkingView extends StatelessWidget {
  const CoworkingView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Coworking'));
  }
}
