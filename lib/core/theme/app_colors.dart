import 'dart:ui';

class AppColors {
  const AppColors._();

  // Brand
  static const brand = Color(0xFF002E5D);

  // Text hierarchy
  static const textPrimary = brand;
  static const textSecondary = Color(0xFF344054);
  static const textSubtitle = Color(0xFF667085);

  // Background
  static const scaffoldBackground = Color(0xFFF7F9FC);

  // Border
  static const borderLight = Color(0xFFE6EAF0);
  static const borderDefault = Color(0xFFD0D5DD);

  // Others
  static const tabUnselectedLabel = Color(0xFFBCC1CB);
  static const cardShadow = Color(0x1F101828);
  static const selectedBackground = Color(0xFFE7EEF7);

  static const gray = Color(0xFF98A2B3);
  static const dialogShadow = Color(0x1A101828);
}
